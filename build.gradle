apply plugin: 'java'
apply plugin: 'application'

mainClassName = 'org.rpmoore.algorist.Main'

repositories {
  mavenCentral()
}

dependencies {
  testCompile 'junit:junit:4.12'
}

task wrapper(type: Wrapper) {
  gradleVersion = '3.0'
}
